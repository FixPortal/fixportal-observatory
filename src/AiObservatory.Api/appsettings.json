{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning",
      // 425,118 records / 0.842 GB over the 7 days to 2026-08-08 — every SQL
      // statement with its text and parameter values. See the matching comment in
      // AiObservatory.Ingest/appsettings.json; together the two apps' EF logging
      // was the single largest line in the workspace.
      "Microsoft.EntityFrameworkCore.Database.Command": "Warning"
    }
  },
  "AllowedHosts": "*"
}
